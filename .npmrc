# Publish all workspace packages as public by default
access=public

# Require exact engine versions declared in package.json
engine-strict=false

# Do not hoist peer dependencies silently
legacy-peer-deps=false

# Lockfile version 3 (npm 7+)
lockfile-version=3
