apiVersion: v1
kind: Service
metadata:
  name: {{ include "consenti.fullname" . }}
  labels:
    {{- include "consenti.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consenti.selectorLabels" . | nindent 4 }}
