# consenti

A minimal Helm chart for self-hosting `@consenti/api`.

**Community reference chart — not covered by CI, tests, or support commitments.** See
[`../../README.md`](../../README.md) for full usage instructions, and
[`values.yaml`](./values.yaml) for every configurable field.
