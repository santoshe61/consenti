# consenti (Terraform module)

Deploys the [`../helm/consenti`](../helm/consenti/) chart via `helm_release`, plus a
`kubernetes_secret` for admin credentials.

**Community reference module — not covered by CI, tests, or support commitments.** See
[`../README.md`](../README.md) for full usage instructions and [`variables.tf`](./variables.tf)
for every input.
